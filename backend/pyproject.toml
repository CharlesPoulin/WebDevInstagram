[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic-settings",
    "pydantic[email]",
    "email-validator",
    "psycopg2-binary",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
